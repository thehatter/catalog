= form_for(@category) do |f|
  = f.hidden_field :parent_id

  p.panel 
    = f.label "Название категории"
    = f.text_field :name
 
    = f.label "Адрес"
    = f.text_field :url

    = f.label "Вес"
    = f.text_field :weight

  = f.submit "Сохранить категорию", class: "success button"